Prompt Atelier ギャラリー4枚だけ反映 軽量版 v86

このフォルダ名ごとではなく、中のファイルをGitHubの同じ場所へ上書きしてください。

入っているファイル:
- index.html
- src/App.tsx
- src/App.compiled.js
- src/data/sampleSeed.json
- public/samples/gallery/ の画像4枚

今回は重い root の sampleSeed.json / data/sampleSeed.json / dist は入れていません。
アプリが実際に読み込むのは src/data/sampleSeed.json です。

確認済み:
- galleryItems は4件
- indexeddb: 参照は0件
- 画像4枚は実ファイルとして同梱